COMMENTS SCANNED:            2632
AGREEMENT LANGUAGE ANYWHERE: 230   (the loose pass — mostly narrative and CSS notes)
LIVE AGREEMENT CLAIMS NAMING >=2 ARTEFACTS: 48

BY SHAPE: mirrors=17 · matches=11 · SAME source=8 · identical=5 · parity=4 · in sync=2 · agree=1

· [in sync] in `(top level)`  — names: mode-draft, archive-sheet, Blueprint.html
    ANCHOR "══ U3 — LIVING-LEDGER 'SHEET' (mode-draft) ══════════════════════════════════════════════ PAPER = verbatim Blueprint-Archive .archive-sheet treatment (Blueprint.html"
· [mirrors] in `(top level)`  — names: capital-authority, slider-portfolio
    ANCHOR "S2.1 — Total Portfolio / Annual Contributions boxes become a stale mirror once rooms drive the Shape (G5 capital-authority overwrites slider-portfolio every render)."
· [agree] in `name`  — names: metric-row, z-index
    ANCHOR "The donor agrees (its own .metric-row is z-index 20, above this)."
· [mirrors] in `name`  — names: hud-analysis, visible-toggle
    ANCHOR "mirrors the .hud-analysis visible-toggle."
· [parity] in `name`  — names: holdings-modal, telemetry-card
    ANCHOR "DATUM INTELLIGENCE — holdings-modal narrative (Sketch telemetry-card parity)"
· [mirrors] in `name`  — names: shape-panel-svg, grid-tick, grid-label
    ANCHOR "#7: Want-face Y-axis grid (mirror #shape-panel-svg .grid-tick/.grid-label so the ported loop renders)"
· [mirrors] in `name`  — names: state-tint, shape-panel-svg
    ANCHOR "#4b-(ii): Want canvas state-tint — mirror #shape-panel-svg's OVEREXTENDED red wash"
· [mirrors] in `_roomCardInner`  — names: data-driven, per-wing
    ANCHOR "ONE data-driven constant drives the per-wing L1→L2 tree (L48: like the §7 lookup mirror)."
· [identical] in `_pickGroundOwner`  — names: propPurpose, byte-identical
    ANCHOR "Returns null when it must not fire — callers assign only on a truthy * result, so a property that is not the first is born with NO propPurpose key at all, byte-ident"
· [mirrors] in `_di402gLimits`  — names: catch-up, superCU
    ANCHOR "NO super catch-up (superCU=0, kept only to mirror the 402(g) shape"
· [mirrors] in `_di402gLimits`  — names: most-recent, _di402gLimits
    ANCHOR "FLAG (R79): unknown future year falls back to the most-recent row (mirrors _di402gLimits);"
· [SAME source] in `hasNum`  — names: secondStockRow, value-weighted
    ANCHOR "secondStockRow rides the SAME value-weighted pass (Instrument=Stock only, dollar"
· [identical] in `_diIraSpine`  — names: _diTaxSignals, byte-identical, _diTaxArchetype
    ANCHOR "_diTaxSignals(acc,s) and the trigger ladder is byte-identical to _diTaxArchetype — only the COPY is the"
· [mirrors] in `_diIraContrib`  — names: instrument-mix, _diTaxInstTag
    ANCHOR "instrument-mix sentence (mirroring Taxable §3b _diTaxInstTag)? Not fabricating one (L47)."
· [matches] in `_diNarrIRA`  — names: _studio_source, stripComments, oracle-validated
    ANCHOR "(1) Every matcher in scripts/ strips comments before matching (_studio_source.cjs stripComments, oracle-validated over 12,553 comments)."
· [matches] in `_diBlankNudge`  — names: baseId, instrument-mix
    ANCHOR "── TAXABLE bank ('The Living Room', baseId taxable) — a DIFFERENT machine from the [R]/[T] banks: pick-ONE spine (10 archetypes, first match) + §3b instrument-mix ta"
· [matches] in `closeCBEstimator`  — names: first-match, pre-spine
    ANCHOR "§3a COMPOSITION ARCHETYPE (Taxable bank R281–R292) — a first-match pre-spine that NAMES what the"
· [parity] in `_diAccountValueCell`  — names: tax-lot, tax-advantaged
    ANCHOR "§19 tax-lot COLUMN hovers for tax-advantaged rooms (authored §Rollup Parity, install VERBATIM)."
· [matches] in `_diBankStrip`  — names: branch-agnostic, sourced-or-blank
    ANCHOR "§8 401(k) EMPLOYER-MATCH ENGINE (STEP 1a · branch-agnostic · Lesson 47 sourced-or-blank)."
· [matches] in `_diBankStrip`  — names: matchRate, matchUpTo, vestedPct, matchBalance
    ANCHOR "Consumes the modal match fields (acc.matchRate / matchUpTo / vestedPct / matchBalance), the"
· [matches] in `num`  — names: rothBalance, pre-tax, profit-sharing
    ANCHOR "rothBalance is the remainder after ALL pre-tax employer/rolled buckets (match + profit-sharing +"
· [matches] in `pctOf`  — names: value-weighted, first-match
    ANCHOR "§3a COMPOSITION ARCHETYPE (R258–R274) — value-weighted, first-match;"
· [matches] in `_di401kMenuFees`  — names: match-split, _di401kMatch
    ANCHOR "LAYER E · TAX & STRUCTURE (R137–R139) — match-split now REAL (from _di401kMatch STEP 1a);"
· [matches] in `_di401kTax`  — names: pre-tax, roth-only, matchBalance
    ANCHOR "is one pre-tax pool, so the match 'simply blends in' (no roth-only {matchBalance} token)."
· [matches] in `opt`  — names: hoType, stop-and-flag
    ANCHOR "* ⚠️ AN hoType NOT IN THIS MAP RENDERS NOTHING and is a stop-and-flag, never a nearest match."
· [mirrors] in `_propEndorsements`  — names: three-part, _upkeepMirrorField, read-only
    ANCHOR "══ §45.3 · ONE TYPED COST FIELD, OR ITS READ-ONLY MIRROR ═════════════════════════════════════ * Same three-part shape as _upkeepMirrorField directly below (L48 — re"
· [SAME source] in `_diIntelligence`  — names: re-runs, startRate
    ANCHOR "§18.B CAP-BOUNDED WORST-CASE BAND — re-runs the SAME amortization at the ceiling rate (startRate +"
· [mirrors] in `_num`  — names: read-only, independently-stored
    ANCHOR "carry field is a read-only MIRROR of it, never a second independently-stored number that could drift or"
· [SAME source] in `_propInsuranceExtras`  — names: calcCarryTotal, realMonthly
    ANCHOR "calcCarryTotal is deliberately NOT restructured — every consumer (§4.16, the Yard's realMonthly, the Moat) keeps reading the same shape it always did."
· [SAME source] in `_groundsAvmSnapshot`  — names: pickCoords, off-globe
    ANCHOR "Validated to the SAME rule as the Worker's pickCoords — a half pair or an off-globe number is not a location (L48, one definition, never two)."
· [identical] in `_groundsAvmStatusHTML`  — names: just-fetched, byte-identical
    ANCHOR "reopened snapshot and a just-fetched one are byte-identical on screen."
· [in sync] in `wing`  — names: updateAccField, re-render
    ANCHOR "rendered once on open and updateAccField does not re-render it — so keep this in sync by hand)."
· [identical] in `_moatPieBlockHTML`  — names: state-blind, byte-identical
    ANCHOR "on the data condition, never on the payoff state: the pie is state-blind (byte-identical under OK /"
· [mirrors] in `_updateSVGsCoreImpl`  — names: fmtCap, studio.html
    ANCHOR "directly (inline fmtCap, mirror studio.html:5501) — do NOT dispatch the slider"
· [identical] in `addInstance`  — names: non-first, propPurpose
    ANCHOR "Assigned only when truthy, so a non-first property is born with NO propPurpose key — identical"
· [mirrors] in `_wantPanBlocked`  — names: rotateY180, wantX, startX, clientX
    ANCHOR "mirrored X (rotateY180): wantX = startX - clientX"
· [mirrors] in `cleared`  — names: _scenarioFromInputs, exactVal, click-to-type
    ANCHOR "_scenarioFromInputs reads dataset.exactVal FIRST for the three log sliders and only falls back to the slider position when it is absent, and the markup ships none of"
· [matches] in `_studioBp`  — names: sl-plan-through, month-survival
    ANCHOR "Written here — after the DOB, with sl-plan-through already holding the matching PTA age — month-survival keeps this VERBATIM instead of rebuilding it."
· [SAME source] in `_buildRequestErrorTarget`  — names: slider-datum, exactVal, _scenarioFromInputs, spend-input
    ANCHOR "Reads the SAME slider-datum/exactVal source as _scenarioFromInputs so the engine request, HUD and canvas always agree (spend-input is a mirror only)."
· [SAME source] in `_validateDateField`  — names: blank-means-married, location-means
    ANCHOR "That is the same shape as blank-means-married and location-means-Florida, arriving through a typo instead of a default."
· [mirrors] in `_validatePriDob`  — names: ret-date-warn, plan-end
    ANCHOR "DOB never owns ret-date-warn, so always refresh the plan-end mirror from the"
· [SAME source] in `_stuLeaveAnswered`  — names: buildStudioRequest, slider-datum, exactVal, _scenarioFromInputs
    ANCHOR "⚠️ AND buildStudioRequest ALREADY CLAIMED THIS CONTRACT IN PROSE: 'Read the SAME slider-datum/exactVal source as _scenarioFromInputs so the engine request, HUD, and "
· [mirrors] in `_mountDatumShape`  — names: log-scale, exactVal
    ANCHOR "Mirror drag into the sketch datum slider (sets log-scale position + exactVal in $)."
· [parity] in `invented`  — names: co-architect, plan-end-age
    ANCHOR "── PLAN-THROUGH PARITY — the co-architect's date DERIVES too (2026-09-05) ────────────────── * #plan-end-age has a derivation, a slider behind it, a warn host and a "
· [mirrors] in `applyDossierSliders`  — names: spend-input, slider-datum
    ANCHOR "── 04/ Datum mirror: keep spend-input in agreement with slider-datum ──"
· [parity] in `wireSliders`  — names: sketch.html, plan-through
    ANCHOR "Item 1 — Sketch parity (sketch.html:5031): plan-through floor = max(75, RA+20)."
· [mirrors] in `init`  — names: _datumSeedDossier, reachable-empty
    ANCHOR "_datumSeedDossier resolves null for BOTH 'no session' * and 'session, but D1 unreachable and no Clerk mirror' — two different answers * that must not share a branch "
· [SAME source] in `activeMeta`  — names: _placeInNet, overwrite-confirm
    ANCHOR "Same store call the picker already uses for an overwrite (L48 reuse — no new store path, no new mechanism): save() adopts opts.blueprint_id and _placeInNet then upda"
