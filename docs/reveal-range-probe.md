# Reveal Range — what I need you to catch for me

The panel itself is fine. I opened it directly, past everything else, and it drew perfectly.
So whatever is going wrong happens **between the server answering and the panel opening**, and
which way it goes depends on what the server sends back. I can't read that from here — it only
exists while you're pressing the button.

Five steps. Two minutes.

---

### 1. Open the Studio in Chrome and press **F12**

A panel opens on the right or bottom. That's the recorder.

### 2. Click the tab at the top of that panel that says **Console**

Then click the small **circle-with-a-line-through-it** icon just under it — that clears out
anything old, so what you catch is only from your run.

### 3. Leave it open. Build your household as normal and press **Reveal Range**

Don't close the panel. Let the swarm play all the way through until you're back at your rooms.

### 4. Click the tab that says **Network**, then the one that says **Fetch/XHR**

You'll see a short list of rows. **One of them will be red, or will say something other than 200
in the Status column.** That's the one I need.

### 5. Send me three things

- A screenshot of the **Console** tab (step 2) — everything in it, even if it looks empty.
- A screenshot of the **Network → Fetch/XHR** list, showing the Status column.
- Click the row whose status is **not** 200, choose **Response** on the right, and screenshot that.

---

**If every row says 200 and the Console is completely empty, tell me that.** That is not a
failed errand — it eliminates the half of this I can't otherwise rule out, and it's the single
most useful sentence you could send me.

**One small extra, only when you happen to build a household anyway:** save one where you take
Social Security at **62 or 70** instead of 67. One of my checks can't tell a real answer from a
default while every saved file I have claims at 67. Not a task — just whichever one you build next.
