# Deterministic line endings. All text normalized to LF in the repo AND the
# worktree (eol=lf overrides core.autocrlf=true). This kills the CRLF flip that
# false-failed the byte-equality parity gates twice this saga. (CLAUDE.md D27)
* text=auto eol=lf

# Binary assets — never normalize (belt-and-suspenders over git auto-detection).
*.png  binary
*.jpg  binary
*.ico  binary
*.xlsx binary
